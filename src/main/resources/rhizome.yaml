##
# Rhizome Bootstrap Configuration
##

# Set to false to disable persistence
enable-persistence:  false
session-clustering-enabled: false

hazelcast:
    server: true
    group: "local"
    password: "trellis"
    cp-member-count: 0
    seed-nodes:
        - "localhost"
    port: 5710
hazelcast-clients:
    IDS:
        server: false
        group: "local"
        password: "trellis"
        cp-member-count: 0
        seed-nodes:
            - "127.0.0.1:5710"
postgres:
    citus: false
    hikari:
        jdbcUrl: "jdbc:postgresql://localhost:5432/chronicle"
        username: "oltest"
        password: "test"
        maximumPoolSize: 10
datasources:
    chronicle:
        flavor: redshift
        citus: false
        initialize-tables: true
        hikari:
            jdbcUrl: "jdbc:redshift://data.cyvx8udwo1l2.us-gov-west-1.redshift.amazonaws.com:5439/chronicle_dev"
            username: "chronicle_dev"
            password: "...."
            maximumPoolSize: 100
